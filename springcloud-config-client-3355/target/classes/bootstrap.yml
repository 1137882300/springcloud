# 根加载器，系统级别的配置
spring:
  cloud:
    config:
      uri: http://localhost:3344
      name: config-client # 需要从git上读取的资源名字，不要后缀
      profile: test
      label: master
      # 拼接起来就是：http://localhost:3344/config-client-dev/master






